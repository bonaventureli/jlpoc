0.5本版：
计划增加接收BM CAN信号功能
CAN请求一次状态，就下发一下车辆状态,不请求就不发送CAN状态，但MCU是通过AT指令不断请求蓝牙状态

0.3版本：
把蓝牙连接和车内车外状态打包一起发送，没有单独发送蓝牙连接上的状态
三种状态：断开，车内，车外
取消AT+FOB_FIND请求消息
只保留AT+CAR_REQ车辆状态消息
修改CAN bug

0.4版本
Task2文件中删掉不必要的注释代码
case 15:
case 32:
上面两个case处理的功能一样，case 15是AT+INCAR, case 32是AT+CARIN
兼容蓝牙发过来的不同数据。

case 16:
case 33:

case 16是功能AT+OUTCAR
case 33是功能AT+OUTCAR_NEAR
为了兼容蓝牙发来的不同数据，上面两个case实现的功能一样

case 31:
case 35:
兼容AT+DISCONNECT和AT+FOB_NO两个指令，功能一样

测试蓝牙固件:两种固件都支持
